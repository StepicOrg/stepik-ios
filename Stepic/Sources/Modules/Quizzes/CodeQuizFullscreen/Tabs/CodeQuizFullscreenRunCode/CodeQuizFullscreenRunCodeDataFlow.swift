import Foundation

enum CodeQuizFullscreenRunCode {
    /// Sets default sample (stdin)
    enum UpdateSampleInput {
        struct Response {
            let input: String
        }

        struct ViewModel {
            let input: String
        }
    }
}
